import * as React from 'react';
import {Searchbar} from 'react-native-paper';

const MyComponent = props => {
  return <Searchbar placeholderTextColor={'grey'} {...props} />;
};

export default MyComponent;
